/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  eslint: {
    ignoreDuringBuilds: true,
  },
  images: { unoptimized: true },
  env: {
    NEXT_FONT_GOOGLE_MAX_RETRIES: '3'
  }
};

module.exports = nextConfig;